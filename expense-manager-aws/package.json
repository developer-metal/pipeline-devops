{
  "name": "expense-app-manager-ts",
  "version": "1.0.0",
  "main": "src/spend.ts",
  "scripts": {
    "build": "esbuild src/spend.ts --bundle --platform=node --target=node18 --outfile=dist/spend.js",
    "test": "jest"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/jest": "^29.5.3",
    "@types/node": "^24.3.1",
    "esbuild": "^0.27.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.887.0",
    "@aws-sdk/lib-dynamodb": "^3.887.0"
  }
}
